{
  "name": "firebase-test-stenciljs",
  "private": true,
  "version": "0.0.1",
  "description": "firebase-test-stenciljs",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch"
  },
  "dependencies": {
    "@firebase/app": "^0.3.13",
    "@firebase/auth": "^0.9.6",
    "@firebase/firestore": "^1.1.1",
    "@firebase/storage": "^0.2.12",
    "@ionic/core": "^4.1.2"
  },
  "devDependencies": {
    "@stencil/core": "0.18.0"
  }
}
